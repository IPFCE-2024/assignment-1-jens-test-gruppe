#include <stdio.h>

int main() {

    printf("Type three integers to be added:\n");
    int x, y, z;
    scanf("%d%d%d", &x, &y, &z);
    int sum = x + y + z;
    printf("the sum is (%d)\n", sum);

    return 0;
    return 0;
}
